Bad Apple in rust compile errors using proc macros.

## Usage (Building?)
```sh
unzip frames.zip

while true
do
    cargo build
done

# zoom out / decrease the terminal emulator's font size
```

## Why?
Why not
